---
- name: Setup OS
  hosts: all
  roles:
    - role: configure_os
      vars:
        configs:
          - uninstall_applications
          - install_applications
          - ssh_keys
          - set_global_gitignore
