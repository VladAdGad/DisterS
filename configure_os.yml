---
- name: Setup OS
  hosts: all
  roles:
    - role: configure_os
      vars:
        configurations:
          - uninstall_applications
          - install_applications
          - ssh_keys
          - set_global_gitignore
